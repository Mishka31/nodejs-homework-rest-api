const register = require('./register')
const login = require('./login')
const logout = require('./logout')
const updateImg = require('./updateIMG')
const verify = require('./verify')

module.exports = { register, login, logout, updateImg, verify }
